# Example MCP Search Server

## Tools

- `search_docs`: query indexed documentation.
- `open_doc`: return one document by id.

## Permissions

Both commands are read-only.

## Data

The server reads local markdown files and returns matching snippets.
